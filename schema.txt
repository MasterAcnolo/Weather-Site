On rentre le nom d'une ville -> Recherche intuitive

On valide notre ville -> On GET LAT et LON

On les mets en paramètres de l'API et puis on fait la requete

L'API renvoie les infos que l'on veut

-------------------------------------------

api.openweathermap.org/data/2.5/forecast?lat={lat}&lon={lon}&appid={API key}